# 大樂透選號 APP — 產品需求文件（PRD）

## 專案概述
一款模仿股票智能選股 App 介面風格（卡片式篩選、深色主題、黃色強調色）的台灣彩券大樂透選號輔助工具。第一版鎖定 iOS（SwiftUI），未來視情況評估跨平台（Flutter）以支援 Android。

## 設計參考
- UI 風格參考股票篩選 App：深色背景、卡片式功能按鈕、黃色（#確認/強調）與紅色（角標數字）搭配
- **單一首頁、無分頁**：所有選號規則以可勾選清單呈現，使用者可單選或複選多個規則，按「產生號碼」後將結果彙總顯示於頁面最上方

## 全域設定
- **候選號碼數量 N**：使用者可自訂每種選號功能產出的號碼數量，範圍 **6～10**，全域共用一組設定值（建議用 `@AppStorage` 儲存），介面可用 Segment 或 Stepper 呈現，風格比照參考截圖的按鈕列。

---

## 功能規格

### 選號規則清單（單一首頁，可複選）
所有規則皆以勾選項目呈現於同一首頁，可單獨勾選一項，也可同時勾選多項。多項規則同時勾選時，採「輪流從各規則候選號碼中取號去重湊滿 N 個」的方式合併，避免排序在前的規則獨佔結果；不足 N 個時，以所有已選規則的種子值合併，決定式地補滿。使用者按下「產生號碼」後，結果統一顯示於頁面最上方。

| 規則 | 說明 |
|---|---|
| 前 N 大常開獎號碼 | 依歷史全部資料統計出現頻率最高的 N 碼 |
| N 個最少開獎號碼 | 出現頻率最低的 N 碼 |
| 拖牌預測 | 依「前 50 期」走勢，計算遺漏值（距上次開出的期數），推薦 N 個「該出現」的號碼 |
| 生日 + 星座幸運號 | 依使用者輸入生日換算星座，搭配對應數字規則產出候選號碼 |
| 吉時幸運號 | 依當下時辰（吉時對應表，需另外確認規則來源）產出候選號碼 |
| 生肖/五行幸運號 | 依生日換算生肖與五行屬性，產出候選號碼 |
| 姓名筆劃選號 | 輸入姓名，依筆劃數計算轉換出候選號碼 |
| 無腦選號（開發中追加） | 使用者輸入任意一段文字（內容不限，講幹話、發洩心情都行），以穩定字串雜湊轉換出號碼；內容本身無意義，是獨立於其他規則之外的純娛樂彩蛋功能 |

### 附加統計分析（開發討論中提出，可列入後續版本）
- 冷熱號切換偵測
- 號碼區間分布（大小區 / 單雙比）
- 連號分析
- AC 值（號碼分散度）
- 和值分析
- 同期歷史比對（同一天/同節氣歷年開獎統計）

> 註：以上功能皆為純本地端歷史資料統計，不需後端伺服器，可視開發時程排入 MVP 之後的迭代版本；單一首頁架構下，未來會以清單新增規則項目或另闢分析區塊的方式呈現，尚待設計。

### 已排除功能
- ~~熱門組合排行~~：需要使用者帳號系統與後端資料庫記錄他人選號，複雜度高，暫緩實作。
- ~~歷史開獎查詢（獨立查詢頁）~~：台灣彩券官網無正式對外開放 API，法律與維護風險評估後決定取消此獨立查詢頁面。選號建議功能所需的歷史開獎資料仍保留，僅取消「依期別/日期查詢」的獨立瀏覽介面。

### 歷史開獎資料來源
選號建議（常開/最少/拖牌預測）所需的歷史開獎資料，改為呼叫台灣彩券官網本身在用、但未正式對外開放文件的公開查詢端點取得（`api.taiwanlottery.com`），不再使用本地假資料：
- `scripts/update_lottery_data.py`：抓取腳本，寫出 `docs/lotto649.json`
- GitHub Actions（`.github/workflows/update-lottery-data.yml`）：排程自動執行抓取並提交更新
- App 內建 `LotteryApp/Resources/lotto649.json` 作為離線基礎資料，啟動時額外嘗試連網抓取 GitHub 上的最新版本並快取
- 因端點非正式開放、無官方文件保證穩定性，官網改版可能導致抓取失效，需自行承擔並視情況維護

---

## 免責聲明（App 內需呈現）
大樂透開獎結果完全隨機，本 App 提供之統計分析與選號建議僅供參考娛樂，不保證中獎機率。

## 待確認事項
- [ ] 吉時對應數字的規則來源（時辰對應表）
- [ ] 生肖/五行對應數字的規則來源
- [ ] N 值預設值（建議 6，貼近實際下注張數）

## 技術棧
- 平台：iOS（第一版），未來評估 Flutter 做 Android 版
- 框架：SwiftUI
- 資料儲存：本地端為主（Core Data 或 SwiftData），全域設定用 `@AppStorage`
